# Hefferon Linear Algebra id-ID — Durable Task and Recovery

Updated: 2026-08-20T15:39:05+02:00

## Canonical user instruction

The entire file below, including its preface and original dictation, is the user's
instruction for this lane:

- `%USERPROFILE%\Documents\Obsidian notes\Untitled 1693.md`
- 10,476 bytes
- SHA-256 `cf913e8cb4d487f4c6958c079b372ccbb2fb5929dd483068441e80cefd6794f2`

After any context loss, read that exact file, this document, `CURRENT_STATE.md`,
`CURRENT_CURSOR.md`, `DECISION_LOG.md`, `TERMINOLOGY.csv`, and
`ADVERSE_LEDGER.csv`. Continue from their exact recorded cursor. Do not restart
or substitute a summary for these controls.

## Bounded lane

This task owns only R005/B40: Jim Hefferon's *Linear Algebra*, including the
main textbook, complete worked-answer book, and relevant Sage material. Produce
a natural Bahasa Indonesia edition and a locale-neutral modular backend. Do not
coordinate other curriculum lanes.

## Required workflow

1. Perform one short authority preflight: bind the official source site/repo,
   exact edition/tag/commit/archive, source closure, textbook/answer-book/Sage
   assets, identifiers, hashes, build instructions, and exact license route.
2. Begin contiguous reader-facing translation immediately after the authority
   is bound. Translation is primary; backend and QA accompany rather than
   replace it.
3. Preserve mathematics, logical force, notation, formulas, theorem/proof
   structure, exercises, hints, answers, complete solutions, Sage code, figures,
   cross-references, identifiers, and source topology. Never silently rewrite.
4. Log mathematically certain source defects and target corrections separately
   with exact evidence. Maintain durable terminology choices and rejected forms.
5. Build the modular backend required by the shared v0 envelope at
   `outputs/01a01ec1-e685-70d0-b022-211396334723/curriculum_logbook/
   05_MODULAR_BACKEND_INTEROPERABILITY_V0.md`.
6. Produce editable source, reproducible PDF, machine registry, offline closure,
   exact manifests, and proportionate structural/math/language/visual QA.
7. Maintain one bounded Indonesian Hefferon mirror/publication and a handoff for
   the future central curriculum hub.

## Backend minimum

Persist locale-neutral resource, edition, unit, concept, segment, term, asset,
relation, rights, correction, QA, and artifact identities. Bind exact source
locators/hashes, unit hierarchy/order, concept/prerequisite relationships,
exercise–hint–answer–solution links, localized segments, code/assets, component
licenses, corrections, build events, and artifact manifests. Export
deterministic schema-versioned JSON/JSONL/CSV or a lossless equivalent with
round-trip validation. The same source units must map cleanly to Indonesian,
Chinese, or another language.

## Publication and upstream boundaries

The user authorizes publishing substantial bounded completed results without
another permission exchange. The current workspace `AGENTS.md` makes an
explicit publish/push request final authorization and forbids a redundant
confirmation pause. Never run broad Git scans or credential hunts. Record every
publication transaction and verify public bytes anonymously.

Do not contact upstream during production. Only after the entire assigned corpus
is complete may this lane send at most one concise, deduplicated, precise,
high-confidence issue if permitted. Say Codex acted on the user's request; state an
exact model only if runtime-verified; do not start a chatbot conversation.

## Completion

Complete only when textbook, worked-answer book, and assigned Sage material are
translated; authority/license/build records and backend are exact; source,
PDF, registry, and manifests reproduce and pass bounded QA; the individual
publication is anonymously verified; the central-hub handoff is ready; and any
upstream issue is filed once or durably declined.
